{
  "name": "Lint on Save",
  "version": "1.0.0",
  "description": "Run TypeScript type checking whenever a source file is saved to catch errors immediately",
  "when": {
    "type": "fileEdited",
    "patterns": ["src/**/*.ts", "src/**/*.tsx"]
  },
  "then": {
    "type": "runCommand",
    "command": "npx tsc --noEmit"
  }
}
