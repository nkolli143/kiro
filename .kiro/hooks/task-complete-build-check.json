{
  "name": "Build Check After Task",
  "version": "1.0.0",
  "description": "After each spec task completes, run a TypeScript check to ensure no type errors were introduced",
  "when": {
    "type": "postTaskExecution"
  },
  "then": {
    "type": "runCommand",
    "command": "npx tsc --noEmit"
  }
}
