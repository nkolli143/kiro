# No API keys required for the core demo!
# EPA ECHO API is public and free
# Nominatim geocoding is free (no key needed)

# Optional: Add a Mapbox token for a map view upgrade
NEXT_PUBLIC_MAPBOX_TOKEN=

# Optional: Add SendGrid for real email delivery
SENDGRID_API_KEY=
SENDGRID_FROM_EMAIL=
